<?xml version="1.0" encoding="UTF-8"?>
<!--
  bean-discovery-mode="none" tells WildFly's Weld (CDI) that this archive
  contains no CDI beans. Without this, WildFly 32 activates its global
  hibernate-validator-cdi interceptor for the deployment, which fails because
  there is no CDI Validator bean (Spring Boot handles validation itself).
-->
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                           https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       version="4.0"
       bean-discovery-mode="none">
</beans>
