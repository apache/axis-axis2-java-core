<?xml version="1.0" encoding="UTF-8"?>

<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one
  ~ or more contributor license agreements. See the NOTICE file
  ~ distributed with this work for additional information
  ~ regarding copyright ownership. The ASF licenses this file
  ~ to you under the Apache License, Version 2.0 (the
  ~ "License"); you may not use this file except in compliance
  ~ with the License. You may obtain a copy of the License at
  ~
  ~ http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing,
  ~ software distributed under the License is distributed on an
  ~ "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  ~ KIND, either express or implied. See the License for the
  ~ specific language governing permissions and limitations
  ~ under the License.
  -->

<!--
  springbootdemo-tomcat11 — Spring Boot + Axis2 demo for Tomcat 11 / Java 21+.
  Shares its Java source with springbootdemo-wildfly, which adds only the
  WildFly WEB-INF descriptors and reads src/main/java from here.

  Build:  mvn clean package
  Output: target/deploy/axis2-json-api/ — an exploded WAR directory, deliberately
          without a .war suffix; see README.md.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>userguide.springboot</groupId>
    <artifactId>axis2-json-api</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>war</packaging>
    <name>axis2-json-api</name>
    <description>Spring Boot with Axis2 demo</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>21</java.version>

        <axis2.version>2.0.2-SNAPSHOT</axis2.version>

        <!-- Exploded WAR handed to the application server; see the header comment. -->
        <deploy.dir>${project.build.directory}/deploy/axis2-json-api</deploy.dir>

        <!--
          Versions spring-boot-dependencies already manages, overridden here by
          setting the BOM's own property.  Do not replace these with a <version>
          on the dependency: that pins the direct dependency only and leaves
          transitive users of the same artifact on the Boot version, which is how
          log4j-jul 2.25.x previously ended up beside log4j-core 2.24.3.
        -->
        <commons-codec.version>1.22.1</commons-codec.version>
        <commons-lang3.version>3.20.0</commons-lang3.version>
        <httpclient5.version>5.6.3</httpclient5.version>
        <httpcore5.version>5.4.3</httpcore5.version>
        <jakarta-servlet.version>6.1.0</jakarta-servlet.version>
        <log4j2.version>2.25.4</log4j2.version>

        <!-- Versions Spring Boot does not manage. -->
        <axiom.version>2.0.0</axiom.version>
        <commons-collections4.version>4.4</commons-collections4.version>
        <commons-fileupload2.version>2.0.0-M5</commons-fileupload2.version>
        <commons-io.version>2.22.0</commons-io.version>
        <commons-validator.version>1.10.1</commons-validator.version>
        <esapi.version>2.7.0.0</esapi.version>
        <stax2-api.version>4.2.1</stax2-api.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <!-- Required in this userguide for
                 org.springframework.dao.DataAccessException
            -->
            <artifactId>spring-boot-starter-data-jpa</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>com.zaxxer</groupId>
                    <artifactId>HikariCP</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.activation</groupId>
            <artifactId>jakarta.activation-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>angus-activation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-xjc</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-log4j2</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-jul</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-collections4</artifactId>
            <version>${commons-collections4.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>${commons-io.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-codec</groupId>
            <artifactId>commons-codec</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-validator</groupId>
            <artifactId>commons-validator</artifactId>
            <version>${commons-validator.version}</version>
        </dependency>
        <!-- Axis2 core (JSON-RPC/MCP only — SOAP deps excluded) -->
        <dependency>
            <groupId>org.apache.axis2</groupId>
            <artifactId>axis2-kernel</artifactId>
            <version>${axis2.version}</version>
            <exclusions>
                <!-- SOAP/WS-* deps not needed for JSON-RPC/MCP -->
                <exclusion><groupId>org.apache.axis2</groupId><artifactId>axis2-saaj</artifactId></exclusion>
                <!-- Codegen/WSDL tools not needed at runtime -->
                <exclusion><groupId>org.apache.axis2</groupId><artifactId>axis2-codegen</artifactId></exclusion>
                <exclusion><groupId>org.apache.axis2</groupId><artifactId>axis2-adb-codegen</artifactId></exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.apache.axis2</groupId>
            <artifactId>axis2-transport-http</artifactId>
            <version>${axis2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.axis2</groupId>
            <artifactId>axis2-transport-local</artifactId>
            <version>${axis2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.axis2</groupId>
            <artifactId>axis2-json</artifactId>
            <version>${axis2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.axis2</groupId>
            <artifactId>axis2-adb</artifactId>
            <version>${axis2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.axis2</groupId>
            <artifactId>axis2-spring</artifactId>
            <version>${axis2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.axis2</groupId>
            <artifactId>axis2-openapi</artifactId>
            <version>${axis2.version}</version>
        </dependency>
        <!-- HTTP/2 Transport -->
        <dependency>
            <groupId>org.apache.axis2</groupId>
            <artifactId>axis2-transport-h2</artifactId>
            <version>${axis2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents.core5</groupId>
            <artifactId>httpcore5-h2</artifactId>
        </dependency>
        <!-- Axiom (XML object model — still needed by kernel internals) -->
        <dependency>
            <groupId>org.apache.ws.commons.axiom</groupId>
            <artifactId>axiom-impl</artifactId>
            <version>${axiom.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.ws.commons.axiom</groupId>
            <artifactId>axiom-jakarta-activation</artifactId>
            <version>${axiom.version}</version>
        </dependency>
        <!-- Deps still needed at runtime (transitive from kernel) -->
        <dependency>
            <groupId>org.codehaus.woodstox</groupId>
            <artifactId>stax2-api</artifactId>
            <version>${stax2-api.version}</version>
        </dependency>
        <!-- commons-fileupload -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-fileupload2-core</artifactId>
            <version>${commons-fileupload2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-fileupload2-jakarta-servlet6</artifactId>
            <version>${commons-fileupload2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.owasp.esapi</groupId>
            <artifactId>esapi</artifactId>
            <version>${esapi.version}</version>
            <classifier>jakarta</classifier>
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents.core5</groupId>
            <artifactId>httpcore5</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents.client5</groupId>
            <artifactId>httpclient5</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
              The openapi module has to reach WEB-INF/modules as openapi-<version>.mar.
              Resolving it as an artifact rather than copying it out of
              ${settings.localRepository} keeps it working with a mirror, a
              non-default repository path, and timestamped remote snapshots.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.8.1</version>
                <executions>
                    <execution>
                        <id>copy-openapi-mar</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.apache.axis2</groupId>
                                    <artifactId>axis2-openapi</artifactId>
                                    <version>${axis2.version}</version>
                                    <type>jar</type>
                                    <outputDirectory>${deploy.dir}/WEB-INF/modules</outputDirectory>
                                    <destFileName>openapi-${axis2.version}.mar</destFileName>
                                </artifactItem>
                            </artifactItems>
                            <overWriteReleases>true</overWriteReleases>
                            <overWriteSnapshots>true</overWriteSnapshots>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
              Each service archive is a jar holding nothing but META-INF/services.xml;
              the implementation classes are loaded from WEB-INF/classes.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>axis2-repository</id>
                        <phase>prepare-package</phase>
                        <configuration>
                            <target>
                                <jar jarfile="${deploy.dir}/WEB-INF/services/Login.aar">
                                    <metainf file="resources-axis2/login_resources/services.xml"/>
                                </jar>
                                <jar jarfile="${deploy.dir}/WEB-INF/services/testws.aar">
                                    <metainf file="resources-axis2/test_service_resources/services.xml"/>
                                </jar>
                                <jar jarfile="${deploy.dir}/WEB-INF/services/BigDataH2Service.aar">
                                    <metainf file="resources-axis2/bigdata_h2_resources/services.xml"/>
                                </jar>
                                <jar jarfile="${deploy.dir}/WEB-INF/services/FinancialBenchmarkService.aar">
                                    <metainf file="resources-axis2/finbench_resources/services.xml"/>
                                </jar>
                                <copy todir="${deploy.dir}/WEB-INF/conf">
                                    <fileset dir="resources-axis2/conf">
                                        <include name="axis2.xml"/>
                                    </fileset>
                                </copy>
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
              war:exploded writes into ${deploy.dir} during package, merging with the
              services, conf and modules put there above.  It is the only producer of
              the deployable directory - do not add a step that unpacks the packaged
              .war on top of it, since at prepare-package that file does not exist yet.
              For Tomcat, copy this directory to webapps/.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.5.1</version>
                <configuration>
                    <webResources>
                        <resource>
                            <directory>src/main/resources</directory>
                            <includes>
                                <include>**/*.xml</include>
                                <include>**/application.properties</include>
                            </includes>
                            <targetPath>WEB-INF/classes</targetPath>
                            <filtering>true</filtering>
                        </resource>
                    </webResources>
                    <webappDirectory>${deploy.dir}</webappDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>exploded</id>
                        <goals>
                            <goal>exploded</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>

    </build>

    <profiles>
        <!--
          Embedded Tomcat profile: adds spring-boot-starter-web so the
          application can run standalone without an external container.

          Usage (must build first, then run from the project directory):
            mvn clean install -DskipTests
            mvn spring-boot:run -Pembedded \
              -Dspring-boot.run.jvmArguments="-Daxis2.repo=target/deploy/axis2-json-api/WEB-INF"

          The default (no profile) build produces a WAR for external
          Tomcat/WildFly deployment with jakarta.servlet-api as provided.
        -->
        <profile>
            <id>embedded</id>
            <dependencies>
                <dependency>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-web</artifactId>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

</project>
